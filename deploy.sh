
# step 1
# NODE_ENV=bscTestnet yarn feed-token:deploy;
# NODE_ENV=bscTestnet yarn feed-token:verify;
# NODE_ENV=baseTestnet yarn feed-vault:deploy;
# NODE_ENV=baseTestnet yarn feed-vault:verify;

# NODE_ENV=baseTestnet yarn doodoo-token:deploy;
# NODE_ENV=baseTestnet yarn doodoo-token:verify;

# NODE_ENV=baseTestnet yarn doodoo-token:deploy;
# NODE_ENV=baseTestnet yarn doodoo-token:verify;
# NODE_ENV=baseTestnet yarn doodoo-token:update-dex-pair;


# NODE_ENV=baseTestnet yarn quest-pool:deploy;
# NODE_ENV=baseTestnet yarn quest-pool:verify;
# NODE_ENV=baseTestnet yarn quest-pool:update-quest;


# NODE_ENV=baseTestnet yarn free-mint-pool:deploy;
# NODE_ENV=baseTestnet yarn free-mint-pool:verify;

# NODE_ENV=baseTestnet yarn token-tool:deploy;
# NODE_ENV=baseTestnet yarn token-tool:verify;

# NODE_ENV=baseTestnet yarn token-tool:verify-token-erc721;


NODE_ENV=opTestnet yarn free-mint-factory:deploy;
NODE_ENV=opTestnet yarn free-mint-factory:verify;


